import NewPostForm from "@/components/post/NewPostForm";

export const metadata = {
  title: "New Post — Modelers Hub",
  description: "Share your scale model build with the community.",
};

export default function NewPostPage() {
  return <NewPostForm />;
}
